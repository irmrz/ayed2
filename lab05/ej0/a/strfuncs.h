#include <stdio.h>
#include <assert.h>

//Calcula longitud de la cadena apuntada por str
size_t string_length(const char *str);

//Devuelve la cadena con los elementos 'c' filtrados
char *string_filter(const char *str, char c);

